export class Quizz {

    constructor(id, title, resource, hasImage = true) {
        this.id = id;
        this.title = title;
        this.resource = resource;
        this.hasImage = hasImage;
    }
  }
